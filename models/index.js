// import the Models and export them so they can be accessed 
const User = require ('./User')
const Reaction = require ('./Reaction')
const Thought = require ('./Thought')

module.exports= { User, Reaction, Thought}